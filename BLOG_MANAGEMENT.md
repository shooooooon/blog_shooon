# Shooon Blog 記事管理ガイド

このドキュメントでは、ブログ記事の追加・編集方法について説明します。

## 記事の追加方法

記事は `client/src/data/posts.ts` ファイルで管理されています。

### 1. posts.ts を開く

`client/src/data/posts.ts` ファイルを開きます。

### 2. 新しい記事オブジェクトを追加

`posts` 配列に新しい記事オブジェクトを追加します。

```typescript
{
  id: "11", // ユニークなID（連番）
  slug: "my-new-post", // URL用のスラッグ（英数字とハイフン）
  title: "新しい記事のタイトル",
  excerpt: "記事の要約文。記事一覧ページに表示されます。",
  content: `# 記事のタイトル

ここに記事の本文をMarkdown形式で書きます。

## 見出し2

段落のテキスト。**太字**や*イタリック*も使えます。

- リスト項目1
- リスト項目2

\`\`\`typescript
// コードブロックも使えます
const greeting = "Hello, World!";
console.log(greeting);
\`\`\`

詳細な内容をここに書いていきます。`,
  date: "2025-03-15", // 公開日（YYYY-MM-DD形式）
  readTime: "5分", // 読了時間の目安
  tags: ["タグ1", "タグ2", "タグ3"] // 記事のタグ
}
```

### 3. ファイルを保存

ファイルを保存すると、開発サーバーが自動的に再読み込みされ、新しい記事が表示されます。

## 記事の編集方法

既存の記事を編集するには、`posts` 配列内の該当する記事オブジェクトを直接編集します。

## 記事の削除方法

記事を削除するには、`posts` 配列から該当する記事オブジェクトを削除します。

## Markdown記法について

記事本文では以下のMarkdown記法が使用できます：

- `# 見出し1` - 大見出し
- `## 見出し2` - 中見出し
- `### 見出し3` - 小見出し
- `**太字**` - 太字
- `*イタリック*` - イタリック
- `[リンクテキスト](URL)` - リンク
- `` `インラインコード` `` - インラインコード
- コードブロック（バッククォート3つで囲む）
- リスト（`-` または `*` で始める）
- 番号付きリスト（`1.` で始める）

## 記事のスラッグについて

スラッグは記事のURLになります。例えば、スラッグが `my-new-post` の場合、記事のURLは `/posts/my-new-post` になります。

**スラッグの命名規則：**
- 英数字とハイフンのみ使用
- 小文字を推奨
- 記事の内容を表す簡潔な名前
- 他の記事と重複しないこと

## タグの活用

タグは記事の分類に使用されます。記事一覧ページでタグによるフィルタリングが可能です。

**タグの例：**
- 技術系: "React", "TypeScript", "JavaScript", "CSS", "フロントエンド", "バックエンド"
- 分野: "Web開発", "パフォーマンス", "テスト", "DevOps"
- その他: "お知らせ", "雑記", "チュートリアル"

## ヒント

- 記事を追加する際は、IDを連番にすることをおすすめします
- 日付は新しい順に並べ替えられるため、順番は気にしなくて大丈夫です
- 記事の要約（excerpt）は、読者が記事の内容を把握しやすいように簡潔に書きましょう
- 読了時間は、記事の長さに応じて適切に設定してください（目安: 400文字/分）

## GitHubでの記事管理

このプロジェクトはGitHubリポジトリと連携しています。記事を更新する際は：

1. GitHubリポジトリをクローン
2. `client/src/data/posts.ts` を編集
3. 変更をコミット＆プッシュ
4. 自動的にデプロイされます

これにより、どこからでも記事の更新が可能です。
