# Shooon Blog 記事管理ガイド

このドキュメントでは、ブログ記事の追加・編集方法について説明します。記事の更新は**Manus経由**と**GitHub経由**の2つの方法があります。

## 記事管理の仕組み

記事は `client/src/data/posts.ts` ファイルで管理されています。このファイルを編集することで、記事の追加・編集・削除が可能です。

**重要:** Manus経由で変更を行った場合も、自動的にGitHubリポジトリに反映されます。逆に、GitHub経由で変更した場合は、Manusで次回チェックポイントを作成する際に反映されます。

---

## 方法1: Manus経由での記事管理

Manusの管理画面から直接ファイルを編集する方法です。

### 手順

1. **Manusの管理画面を開く**
   - 右側のパネルから「Code」タブを選択

2. **posts.tsファイルを開く**
   - `client/src/data/posts.ts` を選択して編集

3. **記事を追加・編集**
   - 下記の「記事の追加方法」を参照して編集

4. **保存して確認**
   - ファイルを保存すると、開発サーバーが自動的に再読み込みされます
   - 「Preview」タブで変更を確認

5. **チェックポイントを作成**
   - 変更が完了したら、チャットで「チェックポイントを作成してください」と依頼
   - **この時点で自動的にGitHubにプッシュされます**

### メリット
- ブラウザだけで完結
- リアルタイムプレビューで確認しながら編集
- Git操作の知識が不要

---

## 方法2: GitHub経由での記事管理

GitHubリポジトリを直接編集する方法です。より柔軟な編集が可能です。

### 前提条件
- Gitの基本的な知識
- GitHubアカウント
- ローカル開発環境（オプション）

### 手順A: GitHub Web UIで編集

1. **GitHubリポジトリにアクセス**
   - https://github.com/shooooooon/blog_shooon にアクセス

2. **ファイルを編集**
   - `client/src/data/posts.ts` を開く
   - 鉛筆アイコン（Edit this file）をクリック

3. **記事を追加・編集**
   - 下記の「記事の追加方法」を参照して編集

4. **コミットして保存**
   - 「Commit changes」ボタンをクリック
   - コミットメッセージを入力（例: "記事を追加: TypeScriptの型システム"）
   - 「Commit changes」で確定

5. **自動デプロイ**
   - プッシュ後、GitHub Actionsが自動的に実行されます
   - Manusで次回チェックポイント作成時に変更が反映されます

### 手順B: ローカル環境で編集

1. **リポジトリをクローン**
   ```bash
   git clone https://github.com/shooooooon/blog_shooon.git
   cd blog_shooon
   ```

2. **依存関係をインストール**
   ```bash
   pnpm install
   ```

3. **開発サーバーを起動**
   ```bash
   pnpm dev
   ```

4. **記事を編集**
   - `client/src/data/posts.ts` をお好みのエディタで編集
   - ブラウザで http://localhost:3000 を開いて確認

5. **変更をコミット＆プッシュ**
   ```bash
   git add client/src/data/posts.ts
   git commit -m "記事を追加: TypeScriptの型システム"
   git push origin main
   ```

### メリット
- 好きなエディタで編集可能
- 複数ファイルを一度に変更可能
- ローカルで動作確認してからデプロイ
- バージョン管理が容易

---

## 記事の追加方法

`posts` 配列に新しい記事オブジェクトを追加します。

### テンプレート

```typescript
{
  id: "11", // ユニークなID（連番）
  slug: "my-new-post", // URL用のスラッグ（英数字とハイフン）
  title: "新しい記事のタイトル",
  excerpt: "記事の要約文。記事一覧ページに表示されます。",
  content: `# 記事のタイトル

ここに記事の本文をMarkdown形式で書きます。

## 見出し2

段落のテキスト。**太字**や*イタリック*も使えます。

- リスト項目1
- リスト項目2

\`\`\`typescript
// コードブロックも使えます
const greeting = "Hello, World!";
console.log(greeting);
\`\`\`

詳細な内容をここに書いていきます。`,
  date: "2025-03-15", // 公開日（YYYY-MM-DD形式）
  readTime: "5分", // 読了時間の目安
  tags: ["タグ1", "タグ2", "タグ3"] // 記事のタグ
}
```

### 追加位置

配列の**最後**に追加してください。日付順の並び替えは自動的に行われます。

```typescript
export const posts: Post[] = [
  // 既存の記事...
  {
    id: "10",
    slug: "testing-strategies-frontend",
    // ...
  },
  // ここに新しい記事を追加
  {
    id: "11",
    slug: "my-new-post",
    // ...
  }
];
```

---

## 記事の編集方法

既存の記事を編集するには、`posts` 配列内の該当する記事オブジェクトを直接編集します。

### 例: タイトルを変更

```typescript
{
  id: "1",
  slug: "welcome-to-my-blog",
  title: "ブログを始めました", // ← ここを編集
  // ...
}
```

---

## 記事の削除方法

記事を削除するには、`posts` 配列から該当する記事オブジェクトを削除します。

**注意:** 削除した記事のURLにアクセスすると404ページが表示されます。

---

## Markdown記法について

記事本文では以下のMarkdown記法が使用できます。

| 記法 | 説明 | 例 |
|------|------|-----|
| `# 見出し1` | 大見出し | `# はじめに` |
| `## 見出し2` | 中見出し | `## 概要` |
| `### 見出し3` | 小見出し | `### 詳細` |
| `**太字**` | 太字 | `**重要**` |
| `*イタリック*` | イタリック | `*emphasis*` |
| `[テキスト](URL)` | リンク | `[Google](https://google.com)` |
| `` `コード` `` | インラインコード | `` `const x = 1` `` |
| コードブロック | 複数行のコード | バッククォート3つで囲む |
| `- 項目` | リスト | `- 項目1` |
| `1. 項目` | 番号付きリスト | `1. 最初` |

### コードブロックの例

````markdown
```typescript
function greet(name: string): string {
  return `Hello, ${name}!`;
}
```
````

---

## 記事のスラッグについて

スラッグは記事のURLになります。例えば、スラッグが `my-new-post` の場合、記事のURLは `/posts/my-new-post` になります。

### スラッグの命名規則

- 英数字とハイフンのみ使用
- 小文字を推奨
- 記事の内容を表す簡潔な名前
- 他の記事と重複しないこと

### 良い例
- `react-hooks-guide`
- `typescript-generics-explained`
- `web-performance-tips`

### 悪い例
- `post1` （内容が不明）
- `React Hooks Guide` （大文字・スペースを含む）
- `記事1` （日本語）

---

## タグの活用

タグは記事の分類に使用されます。記事一覧ページでタグによるフィルタリングが可能です。

### タグの例

| カテゴリ | タグ例 |
|----------|--------|
| 技術系 | React, TypeScript, JavaScript, CSS, HTML |
| 分野 | フロントエンド, バックエンド, Web開発, DevOps |
| トピック | パフォーマンス, テスト, セキュリティ, アクセシビリティ |
| その他 | お知らせ, 雑記, チュートリアル, レビュー |

### タグ設定のコツ

- 1記事あたり2〜5個程度が適切
- 具体的なタグを優先（例: "JavaScript" より "React"）
- 既存のタグを再利用して統一性を保つ

---

## ヒントとベストプラクティス

### IDの管理
記事を追加する際は、IDを連番にすることをおすすめします。最後の記事のIDを確認して、次の番号を使用してください。

### 日付の設定
日付は新しい順に自動的に並べ替えられるため、配列内の順番は気にする必要ありません。公開日を正確に設定することで、読者が記事の鮮度を判断できます。

### 要約文の書き方
記事の要約（excerpt）は、読者が記事の内容を把握しやすいように簡潔に書きましょう。1〜2文程度が理想的です。

### 読了時間の設定
読了時間は、記事の長さに応じて適切に設定してください。目安として、日本語は400文字/分程度です。

**計算例:**
- 2000文字の記事 → 約5分
- 3000文字の記事 → 約7分

### コミットメッセージ
GitHub経由で編集する場合、わかりやすいコミットメッセージを書くことで、後から変更履歴を追いやすくなります。

**良い例:**
- `記事を追加: React Hooksのベストプラクティス`
- `記事を更新: TypeScript記事の誤字を修正`
- `記事を削除: 古くなったDocker記事を削除`

---

## トラブルシューティング

### Q: 記事を追加したのに表示されない

**A:** 以下を確認してください：
1. `posts` 配列に正しく追加されているか
2. カンマ（`,`）が正しく配置されているか
3. 構文エラーがないか（TypeScriptのエラーを確認）
4. ファイルが保存されているか

### Q: コードブロックが正しく表示されない

**A:** バッククォート（`` ` ``）が3つ正しく配置されているか確認してください。また、言語指定（`typescript`, `javascript` など）が正しいか確認してください。

### Q: GitHubにプッシュしたのにManusに反映されない

**A:** Manusで次回チェックポイントを作成すると、GitHubの最新の変更が反映されます。チャットで「最新の変更を取り込んでチェックポイントを作成してください」と依頼してください。

### Q: Manusで変更したのにGitHubに反映されない

**A:** Manusでチェックポイントを作成すると、自動的にGitHubにプッシュされます。チェックポイント作成後、GitHubリポジトリを確認してください。

---

## まとめ

このブログは、ManusとGitHubの両方から記事を管理できる柔軟な仕組みになっています。

- **手軽に更新したい** → Manus経由
- **本格的に編集したい** → GitHub経由

どちらの方法でも、変更は自動的に同期されます。あなたのワークフローに合った方法を選んで、快適にブログを運営してください。

---

**リポジトリURL:** https://github.com/shooooooon/blog_shooon

ご質問や不明点があれば、お気軽にお問い合わせください。
